.env 파일이 제대로 채워졌는지 검사하는 CLI를 만들고 싶어.

- .env.example을 기준으로 삼아서, 실제 .env에 빠진 키가 있으면 알려줘.
- 값이 비어 있는 키도 잡아줘.
- 반대로 .env에만 있고 example에 없는 키도 알려주면 좋겠어.
- 팀에서 쓸 거라서 CI에서 돌릴 수 있어야 해.
- Python으로 만들 거고, 표준 라이브러리만 썼으면 좋겠어.
- 값 자체는 절대 화면에 찍으면 안 돼. 비밀이 로그에 남으면 안 되니까.
